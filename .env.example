NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
ANTHROPIC_API_KEY=
# Optional — overrides the Origin header for auth redirect URLs (magic link / OAuth callback).
# Only needed if requests reach this app through a proxy that strips or spoofs Origin.
NEXT_PUBLIC_SITE_URL=
